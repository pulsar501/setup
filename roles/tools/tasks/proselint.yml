---
# On macOS proselint comes from Homebrew. Linux only.
- name: Install proselint via pipx
  ansible.builtin.shell: pipx install proselint
  changed_when: true
  when: ansible_facts['os_family'] != 'Darwin'
  tags: ["dev", "proselint"]
